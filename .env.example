# Supabase Edge Function URL (optional - defaults to placeholder)
# VITE_WAITLIST_URL=https://your-project.supabase.co/functions/v1/waitlist

# For local development with Docker/Postgres, create a .env file with:
# POSTGRES_PASSWORD=your-local-password
# SUPABASE_DB_PASSWORD=your-local-password